language = C++
# 使用相对路径引用头文件
headers = webrtc_apm_wrapper.h  yanshee.h
# 确保包含正确的库路径和依赖
linkerOpts = -L../cpp/libs -lwebrtc_apm -lpthread -lm
headerFilter = webrtc_apm_wrapper.h  yanshee.h
package = com.airobot.webrtcapminterop
# 下面定义不透明指针类型
---
struct WebRtcApm {}; 